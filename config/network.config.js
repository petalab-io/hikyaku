/**
 * ===========================================================================
 * ネットワーク設定
 * ===========================================================================
 *
 * キャッシュ設定とネットワーク帯域制限を設定します。
 * 必要に応じて変更してください。
 *
 * このファイルはgit管理されます（共通設定として使用）
 * ===========================================================================
 */

module.exports = {
    // キャッシュ設定（true: キャッシュ無効, false: キャッシュ有効）
    disableCache: true,

    // ネットワーク設定
    network: {
        offline: false,
        downloadThroughput: (10 * 1024 * 1024) / 8, // 10 Mbps (ダウンロード速度)
        uploadThroughput: (10 * 1024 * 1024) / 8,   // 10 Mbps (アップロード速度)
        latency: 10,                                 // 10ms (レイテンシ)
    },

    // ========================================
    // プリセット例
    // ========================================
    //
    // ## Fast 3G
    // downloadThroughput: (1.6 * 1024 * 1024) / 8,   // 1.6 Mbps
    // uploadThroughput: (0.75 * 1024 * 1024) / 8,    // 0.75 Mbps
    // latency: 562.5,                                 // 562.5ms
    //
    // ## Slow 3G
    // downloadThroughput: (0.4 * 1024 * 1024) / 8,   // 0.4 Mbps
    // uploadThroughput: (0.4 * 1024 * 1024) / 8,     // 0.4 Mbps
    // latency: 2000,                                  // 2000ms
    //
    // ## 4G
    // downloadThroughput: (4 * 1024 * 1024) / 8,     // 4 Mbps
    // uploadThroughput: (3 * 1024 * 1024) / 8,       // 3 Mbps
    // latency: 20,                                    // 20ms
    //
    // ## WiFi (高速)
    // downloadThroughput: (30 * 1024 * 1024) / 8,    // 30 Mbps
    // uploadThroughput: (15 * 1024 * 1024) / 8,      // 15 Mbps
    // latency: 2,                                     // 2ms
};